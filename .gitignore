# dependências
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# produção
/build

# ambiente — os segredos nunca entram no repositório
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# macOS
.DS_Store

# registos
npm-debug.log*
yarn-debug.log*

# material em bruto (fotografias originais, PDFs da carta, logótipos)
# entra aqui e não no git: são ficheiros grandes que se reprocessam
/originais
